"""Helpers to render the virtual filesystem tree."""

from __future__ import annotations

from .models import Directory


def render_tree(root: Directory) -> str:
    """Return a human-friendly representation of the directory tree."""
    # TODO: Implement tree rendering logic (breadth-first or depth-first).
    raise NotImplementedError
